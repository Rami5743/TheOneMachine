# אנליטיקס מבקרים — GoatCounter

הסבר איך להפעיל מעקב מבקרים (כמה אנשים נכנסים לאתר + פילוחים בסיסיים). GoatCounter
חינמי, בלי עוגיות, ולא אוסף מידע אישי — מתאים לאתר לילדים.

הקוד באתר כבר מוכן ו**רדום**: כל עוד `GOATCOUNTER_CODE` ריק ב-`js/analytics-config.js`,
שום דבר לא נטען ושום נתון לא נשלח. צריך רק לפתוח חשבון ולמלא את הקוד.

---

## שלב 1 — פתיחת חשבון GoatCounter

**כנס ללינק:** https://www.goatcounter.com/signup

**שם תמלא את השדה** "Code" **שנמצא** בראש הטופס — זה יהיה שם הדשבורד שלך
(למשל `theonemachine`, וכתובת הדשבורד תהיה `theonemachine.goatcounter.com`).

**אחר כך תמלא** את השדות "Email address" ו-"Password" **שנמצאים** מתחת ל-Code.
(את "Data in timezone" אפשר להשאיר כברירת מחדל.)

**ואז תלחץ על** הכפתור "Sign up" **שנמצא** בתחתית הטופס.

**לבסוף תגיע** לדשבורד בכתובת `https://<CODE>.goatcounter.com` — ה-`<CODE>` שבחרת
הוא מה שצריך לשלב הבא.

---

## שלב 2 — חיבור האתר לחשבון

יש שתי דרכים (בחר אחת):

**א. תשלח לי את ה-CODE** ואני אמלא אותו בקוד ואדחוף — הכי פשוט.

**ב. לערוך בעצמך:** פתח את `js/analytics-config.js`, והחלף את השורה:
```js
  GOATCOUNTER_CODE: ""
```
ל:
```js
  GOATCOUNTER_CODE: "theonemachine"   // ה-CODE שבחרת
```
שמור, בַּמפ את הגרסה של `analytics-config.js` ב-`index.html` (למשל `?v=v2-code-1`),
ודחוף.

---

## שלב 3 — לראות את הסטטיסטיקות

**כנס ללינק:** `https://<CODE>.goatcounter.com` (עם ה-CODE שלך).

**שם תראה** את לוח המחוונים **שנמצא** במרכז המסך: מספר המבקרים והצפיות, גרף לאורך
זמן, ורשימות של דפים, מקורות הגעה (Referrers), מדינות ומכשירים.

הערות:
- הספירה מתחילה רק **אחרי** שהאתר עם ה-CODE עולה לאוויר (GitHub Pages) — מבקרים
  מקומיים (localhost) לא נספרים.
- זהו אתר עמוד-בודד, אז כל כניסה נספרת כצפייה אחת — מספיק בשביל "כמה נכנסים".

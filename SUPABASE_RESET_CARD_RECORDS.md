# איפוס הדירוגים של כרטיס שדרישותיו השתנו

כשדרישות של כרטיס משתנות, כל מה שנמדד לפי הדרישות הישנות כבר לא תקף: הבנייה
השמורה, ספירת הנאנדים והנאנדים-בטור שנגזרת ממנה, זמן התכנון, וגם עצם השלמת
הכרטיס. צריך לאפס את זה בשני מקומות:

1. **אצל כל שחקן, במשחק עצמו** — נעשה **אוטומטית**. בפעם הראשונה שהוא פותח את
   הגרסה החדשה, המשחק שוכח את הכרטיס: `cardBuilds`, `cardSerialBuilds`,
   `cardNandCounts`, `cardSerialCounts`, `cardDesignCounts`, `taskDesignMs`,
   הרמזים שלו, וההשלמה עצמה (`completedTasks` / `tasksEverCompleted`) — כדי שיבנה
   אותו מחדש לפי הדרישות החדשות. השמירה מסומנת ב-`migrationsDone` כך שזה קורה
   פעם אחת בלבד. אצל שחקן מחובר, הנתונים הנקיים נדחפים לטבלת `rankings` בשמירה
   הבאה, וזה מה שמוריד אותו מלוח השיאים של הכרטיס.
   (הקוד: `cardSpecChanges` / `applyCardSpecChanges` ב-`js/app.js`.)

2. **בענן, לכל המשתמשים הרשומים בבת אחת** — זה **מה שאתה צריך לעשות**, לפי
   ההוראות שלמטה. בלי זה, שיא ישן של מישהו שלא ייכנס למשחק בקרוב ימשיך להופיע
   בראש לוח השיאים של הכרטיס.

הקובץ המלא נמצא ב-`supabase-reset-card-records.sql` שבשורש הפרויקט.

**השינוי הנוכחי**: הכרטיס **ALU4** — הפלאג השני שלו הוחלף מ-nz (1 כשהתוצאה שונה
מ-0) ל-zr (1 כשהתוצאה היא 0).

---

## הוראות הרצה

**כנס ללינק:**
https://supabase.com/dashboard/project/oemhzqjfamqbinikmvfd/sql/new

(אם צריך — התחבר לחשבון Supabase שלך קודם. `oemhzqjfamqbinikmvfd` הוא מזהה
הפרויקט, נלקח מ-`js/auth-config.js`.)

**שם תלחץ על** "SQL Editor" **שנמצא** בסרגל הצד השמאלי (האייקון עם הסימן `</>`).
אם הלינק כבר פתח את עורך ה-SQL — דלג על השלב הזה.

**אחר כך תלחץ על** "+ New query" **שנמצא** בראש סרגל הצד השמאלי, מעל רשימת
השאילתות השמורות (פותח תיבת עריכה ריקה).

**הדבק את הטקסט הבא לתיבת** העריכה הגדולה **שנמצאת** במרכז המסך (לחץ בתוכה ואז
Ctrl+V):

```sql
update public.rankings
set counts     = counts - 'ALU4',
    serial     = serial - 'ALU4',
    design     = design - 'ALU4',
    updated_at = now()
where counts ? 'ALU4'
   or serial ? 'ALU4'
   or design ? 'ALU4';
```

**ואז תלחץ על** "Run" **שנמצא** בפינה הימנית-תחתונה של עורך ה-SQL (כפתור ירוק;
קיצור מקלדת: Ctrl+Enter).

**לבסוף תראה** את ההודעה "Success. Rows returned: 0" (או "No rows returned")
**שנמצאת** בפאנל התוצאות מתחת לעורך — סימן שהצליח. אם תרצה לראות כמה שורות
נוקו, הוסף בסוף השאילתה `returning user_id;` לפני ה-`;` האחרון.

---

## איך בודקים שזה עבד

**כנס ללינק:**
https://supabase.com/dashboard/project/oemhzqjfamqbinikmvfd/editor

**שם תלחץ על** "rankings" **שנמצא** ברשימת הטבלאות בסרגל הצד השמאלי.

**לבסוף תראה** שבעמודות `counts`, `serial` ו-`design` אין יותר המפתח `"ALU4"`
באף שורה.

ובמשחק עצמו: רענון קשה (Ctrl+Shift+R), כניסה למסך "דירוגים" — השורה של ALU4
תהיה ריקה (בלי שיא ובלי דירוג) עד שמישהו יבנה אותו מחדש לפי הדרישות החדשות.

---

## לשימוש חוזר בעתיד

אם דרישות של כרטיס אחר ישתנו:

1. בקוד — להוסיף רשומה ל-`cardSpecChanges()` ב-`js/app.js`
   (`{ id: "<מזהה-חדש>", tasks: ["<שם הכרטיס>"] }`). ה-`id` חייב להיות חדש, אחרת
   השמירות שכבר סומנו לא ירוצו שוב.
2. בענן — להריץ את אותו SQL עם שם הכרטיס החדש במקום `ALU4` (בארבעת המקומות).

# איפוס הדירוגים של כרטיס שדרישותיו השתנו

כשדרישות של כרטיס משתנות, כל מה שנמדד לפי הדרישות הישנות כבר לא תקף: הבנייה
השמורה, ספירת הנאנדים והנאנדים-בטור שנגזרת ממנה, זמן התכנון, וגם עצם השלמת
הכרטיס. צריך לאפס את זה בשני מקומות.

## 1. אצל כל שחקן, במשחק עצמו — אוטומטי

בפעם הראשונה שהשחקן פותח את הגרסה החדשה, המשחק שוכח את הכרטיס: הבנייה השמורה,
שלוש ספירות הדירוג שנגזרות ממנה, שעון זמן התכנון, הרמזים שלו, וההשלמה עצמה — כדי
שיבנה אותו מחדש לפי הדרישות החדשות. השמירה מסומנת כך שזה קורה פעם אחת בלבד.

אצל שחקן מחובר, הנתונים הנקיים נדחפים לטבלת הדירוגים בשמירה הבאה שלו, וזה מה
שמוריד אותו מלוח השיאים של הכרטיס.

הקוד שעושה את זה נמצא ב-`js/app.js`, בפונקציות:

    cardSpecChanges
    applyCardSpecChanges

## 2. בענן, לכל המשתמשים הרשומים בבת אחת — צריך להריץ ידנית

בלי זה, שיא ישן של מישהו שלא ייכנס למשחק בקרוב ימשיך להופיע בראש לוח השיאים של
הכרטיס. ההוראות למטה.

**השינוי הנוכחי**: הכרטיס ALU4 — הפלאג השני שלו הוחלף מ-nz (שהיה 1 כשהתוצאה שונה
מאפס) ל-zr (שהוא 1 כשהתוצאה היא אפס).

---

## הוראות הרצה

### שלב 1 — פתיחת עורך השאילתות

כנס ללינק:

https://supabase.com/dashboard/project/oemhzqjfamqbinikmvfd/sql/new

אם צריך, התחבר קודם לחשבון שלך. מזהה הפרויקט שבלינק נלקח מהקובץ
`js/auth-config.js`.

### שלב 2 — שאילתה חדשה

אם הלינק כבר פתח תיבת עריכה ריקה — דלג לשלב הבא.

אחרת, בסרגל הצד השמאלי, בראש רשימת השאילתות השמורות, לחץ על הכפתור שכתוב עליו:

    + New query

### שלב 3 — הדבקת השאילתה

לחץ בתוך תיבת העריכה הגדולה שבמרכז המסך, והדבק לתוכה את הטקסט הבא:

```sql
update public.rankings
set counts     = counts - 'ALU4',
    serial     = serial - 'ALU4',
    design     = design - 'ALU4',
    updated_at = now()
where counts ? 'ALU4'
   or serial ? 'ALU4'
   or design ? 'ALU4';
```

### שלב 4 — הרצה

לחץ על הכפתור הירוק בפינה הימנית-תחתונה של העורך, שכתוב עליו:

    Run

אפשר גם במקלדת: Ctrl+Enter.

### שלב 5 — סימן ההצלחה

בפאנל התוצאות שמתחת לעורך תופיע ההודעה:

    Success. No rows returned

---

## איך בודקים שזה עבד

כנס ללינק:

https://supabase.com/dashboard/project/oemhzqjfamqbinikmvfd/editor

בסרגל הצד השמאלי, ברשימת הטבלאות, לחץ על הטבלה שכתוב עליה:

    rankings

עבור על השורות. בשלוש העמודות שלמטה כבר לא אמור להופיע המפתח ALU4 באף שורה:

    counts
    serial
    design

ובמשחק עצמו: רענון קשה (Ctrl+Shift+R), ואז מסך הדירוגים. השורה של ALU4 תהיה ריקה
— בלי שיא ובלי דירוג — עד שמישהו יבנה את הכרטיס מחדש לפי הדרישות החדשות.

---

## לשימוש חוזר בעתיד

אם ישתנו דרישות של כרטיס אחר:

1. בקוד — להוסיף רשומה לפונקציה `cardSpecChanges` שב-`js/app.js`, בצורה הזאת:

       { id: "<מזהה-חדש>", tasks: ["<שם הכרטיס>"] }

   המזהה חייב להיות חדש, אחרת שמירות שכבר סומנו לא ירוצו שוב.

2. בענן — להריץ את אותו SQL, עם שם הכרטיס החדש במקום ALU4 בכל ששת המקומות.

הקובץ המלא נמצא ב-`supabase-reset-card-records.sql` שבשורש הפרויקט.

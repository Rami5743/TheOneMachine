# הוספת עמודת `program` לטבלת הדירוגים (דירוגי תוכנה)

**למה זה צריך לקרות.** לשונית "דירוגי תוכנה" (אורך תוכנה) מדרגת משימות תכנות —
למשל "כפל ב-8" (`prg-times-8`). האורך של התוכנה שעברה נשמר אצל השחקן ב-
`state.programCounts`, אבל בטבלת `rankings` ב-Supabase **אין עמודה** שתחזיק אותו,
אז הוא לא נדחף לענן ולא נקרא בחזרה — ולכן ההישג נרשם אבל שיא לא.

הקוד באפליקציה כבר מתוקן ויודע לכתוב ולקרוא את העמודה. נשאר רק ליצור אותה
במסד. **עד שתריץ את זה** האפליקציה עובדת רגיל — היא מזהה שהעמודה חסרה ונופלת
חזרה לשלוש העמודות הישנות, כך שדירוגי החומרה לא נשברים.

---

## 1. הלינק

https://supabase.com/dashboard/project/oemhzqjfamqbinikmvfd/sql/new

## 2. ההוראות

הלינק נוחת ישר על שאילתה חדשה בעורך ה-SQL של הפרויקט, אז אין צורך לבחור פרויקט
ואין צורך ללחוץ על "New query".

הדבק את הטקסט שבסעיף 3 לתיבת עריכת ה-SQL שנמצאת במרכז המסך (התיבה הגדולה
והריקה).

ואז תלחץ על **"Run"** שנמצא בפינה הימנית-תחתונה של עורך ה-SQL (כפתור ירוק;
אפשר גם Ctrl+Enter).

לבסוף תראה **"Success. No rows returned"** שנמצא בחלונית התוצאות מתחת לעורך.

אם בכל זאת נחתת בדף הראשי של Supabase: תלחץ על **הפרויקט של "המכונה האחת"**
שנמצא ברשימת הפרויקטים במרכז המסך, אחר כך על **"SQL Editor"** שנמצא בסרגל
האייקונים האנכי בצד שמאל של המסך (האייקון נראה כמו דף עם הכיתוב `SQL`), ואחר כך
על **"New query"** שנמצא בפינה השמאלית-עליונה של אזור העריכה.

## 3. הטקסט להדבקה

```sql
alter table public.rankings add column if not exists program jsonb not null default '{}'::jsonb;
```

---

## איך לוודא שזה עבד

כנס למשחק בגרסה המבצעית, התחבר עם החשבון שלך, ולך אל **"הישגים"** ← לשונית
**"דירוגי תוכנה"** שנמצאת בקבוצת הלשוניות התחתונה.

בשורה **"כפל ב-8"** אמור להופיע מספר בעמודה **"פקודות"** ובעמודה **"שיא נוכחי"**.

אם עדיין מופיע `—`, פתח את ה-Console של הדפדפן (F12) וחפש שורה שמתחילה ב-
`[leaderboard]`. היא תגיד אם הקריאה או הכתיבה נכשלה ולמה.

**שים לב:** השיא נדחף לענן רק כששומרים מצב אחרי שהמשימה עברה. אם המשימה כבר
עברה לפני שהעמודה נוצרה, פשוט שחק עוד קצת (כל שמירת מצב דוחפת את הדירוגים
מחדש) — או פתח את המשימה, לחץ **"נקה התקדמות"** ופתור אותה שוב.

---

## הערה על רמזים אינטראקטיביים

משימה שהשתמשת בה ברמז אינטראקטיבי — כזה שכותב לך את הפקודות — **יוצאת
מהדירוג בכוונה**: שיא קודם נקי נשמר, אבל שיא חדש לא נרשם. זה מנוהל דרך
`state.rankHintUsed`, ו"נקה התקדמות" על המשימה מנקה את הסימון כך שפתרון עצמאי
חדש יכול להירשם שוב.

אם השיא לא נרשם גם אחרי שהעמודה קיימת, זו הסיבה הסבירה הבאה שכדאי לבדוק.

# הוספת העמודות `runtime` ו-`memory` לטבלת הדירוגים (זמן ריצה ומקום בזיכרון)

**למה זה צריך לקרות.** תחת "דירוגי תוכנה" יש עכשיו שלוש לשוניות: "אורך תוכנה"
(קיימת מזמן, עמודה `program`), וגם **"זמן ריצה"** ו**"מקום בזיכרון"** — שתי
הלשוניות החדשות, שמדרגות את שתי המשימות האחרונות של פרק 5.1 (כפל וחילוק).
המספרים נשמרים אצל השחקן ב-`state.programRuntimeCounts` וב-
`state.programMemoryCounts`, אבל בטבלת `rankings` ב-Supabase **אין עמודות**
שיחזיקו אותם — אז הם לא נדחפים לענן ולא נקראים בחזרה, ולכן עמודות "דירוג"
ו"שיא נוכחי" בשתי הלשוניות האלה יישארו ריקות.

הקוד באפליקציה כבר יודע לכתוב ולקרוא את שתי העמודות. נשאר רק ליצור אותן במסד.
**עד שתריץ את זה** האפליקציה עובדת רגיל — היא מזהה שהעמודות חסרות ונופלת חזרה
לעמודות הקיימות, כך ששאר הדירוגים לא נשברים.

---

## מה לעשות

כנס ללינק: https://supabase.com/dashboard/project/oemhzqjfamqbinikmvfd/sql/new

הלינק הזה נוחת ישר על **שאילתה חדשה בעורך ה-SQL** של הפרויקט של "המכונה האחת"
(מזהה הפרויקט לקוח מ-`SUPABASE_URL` שב-`js/auth-config.js`), כך שאין צורך לבחור
את הפרויקט ולא ללחוץ על "New query". אם בכל זאת נחתת בדף הראשי, תלחץ על
**הפרויקט של "המכונה האחת"** שנמצא ברשימת הפרויקטים במרכז המסך, אחר כך על
**"SQL Editor"** שנמצא בסרגל האייקונים האנכי בצד שמאל של המסך (האייקון נראה
כמו דף עם הכיתוב `SQL`), ואחר כך על **"New query"** שנמצא בפינה
השמאלית-עליונה של אזור העריכה.

הדבק את הטקסט הבא לתיבת עריכת ה-SQL שנמצאת במרכז המסך (התיבה הגדולה והריקה):

```sql
alter table public.rankings add column if not exists runtime jsonb not null default '{}'::jsonb;
alter table public.rankings add column if not exists memory  jsonb not null default '{}'::jsonb;
```

ואז תלחץ על **"Run"** שנמצא בפינה הימנית-תחתונה של עורך ה-SQL (כפתור ירוק;
אפשר גם Ctrl+Enter).

לבסוף תראה **"Success. No rows returned"** שנמצא בחלונית התוצאות מתחת לעורך.

---

## איך לוודא שזה עבד

כנס למשחק בגרסה המבצעית, התחבר עם החשבון שלך, ולך אל **"הישגים"** ← לחץ על
**"דירוגים"**, ואז על קבוצת הלשוניות **"דירוגי תוכנה"** שנמצאת בשורת הקבוצות
מתחת לכותרת — ובתוכה על הלשונית **"זמן ריצה"**.

בשורות **"כפל"** ו**"חילוק"** אמור להופיע מספר בעמודה **"צעדים"**, ואם יש
שיאנים אחרים גם בעמודות **"דירוג"** ו**"שיא נוכחי"**. אותו דבר בלשונית
**"מקום בזיכרון"**, שם העמודה נקראת **"כתובות"**.

אם עדיין מופיע `—`, פתח את ה-Console של הדפדפן (F12) וחפש שורה שמתחילה ב-
`[leaderboard]`. היא תגיד איזו עמודה חסרה או למה הקריאה/הכתיבה נכשלה.

**שים לב:** המספר נדחף לענן רק כששומרים מצב אחרי שהמשימה עברה במכונה. אם
המשימה כבר עברה לפני שהעמודות נוצרו, פשוט שחק עוד קצת (כל שמירת מצב דוחפת את
הדירוגים מחדש) — או פתח את המשימה ופתור אותה שוב.

---

## מה בדיוק נמדד

- **זמן ריצה** — כמה פקודות המכונה ביצעה עד שהתוכנה סיימה, **על כל מקרי הבדיקה
  יחד**. מקרי הבדיקה קבועים ושווים לכולם, אז המספר בר-השוואה.
- **מקום בזיכרון** — בכמה כתובות בזיכרון החופשי (0 עד 1023) התוכנה כתבה, לפי
  מקרה הבדיקה הבזבזני ביותר. Out0–Out3 ו-In0–In3 אינם נספרים: הם הפורטים של
  המכונה, לא מקום שהתוכנה בחרה לתפוס.

שתי הלשוניות מוצגות רק לשתי המשימות האחרונות של פרק 5.1 (`rankRuntime: true`
ב-`DEMO_TASKS` שב-`js/app-data.js`). משימה שנעזרה ברמז אינטראקטיבי יוצאת
מהדירוג בכוונה, בדיוק כמו ב"אורך תוכנה".

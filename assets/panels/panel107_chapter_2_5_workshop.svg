<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg version="1.1" width="1448" height="1086" viewBox="0 0 1448 1086"
   preserveAspectRatio="xMidYMid meet"
   xmlns:xlink="http://www.w3.org/1999/xlink" xmlns="http://www.w3.org/2000/svg">
  <image id="raster-base" x="0" y="0" width="1448" height="1086" preserveAspectRatio="none"
     href="panel107_chapter_2_5_workshop.jpg" xlink:href="panel107_chapter_2_5_workshop.jpg" />

  <!-- Editable click-zones (invisible). Move / resize in Inkscape. The object
       rects (copied from panel99, the same worktable image) link to reference
       articles; the exercise booklet is an action rect wired to the app's own
       hotspot button. -->
  <rect id="hotspot-object-bulbs"
     data-hotspot="object" data-id="bulbs" data-label="נורות חשמליות"
     data-url="https://he.wikipedia.org/wiki/נורה_חשמלית"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="1086" y="817" width="319" height="206" />
  <rect id="hotspot-object-triodes"
     data-hotspot="object" data-id="triodes" data-label="טריודות"
     data-url="https://he.wikipedia.org/wiki/טריודה"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="78" y="763" width="188" height="195" />
  <rect id="hotspot-object-diode"
     data-hotspot="object" data-id="diode" data-label="דיודה"
     data-url="https://he.wikipedia.org/wiki/דיודה"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="24" y="834" width="116" height="141" />
  <rect id="hotspot-object-cable"
     data-hotspot="object" data-id="cable" data-label="כבל חשמלי"
     data-url="https://he.wikipedia.org/wiki/כבל_חשמלי"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="1075.6373" y="559" width="163.3627" height="172.12251" />
  <rect id="hotspot-object-source"
     data-hotspot="object" data-id="source" data-label="מקור מתח"
     data-url="https://he.wikipedia.org/wiki/מקור_מתח"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="1264" y="212" width="130" height="217" />
  <rect id="hotspot-object-voltmeter"
     data-hotspot="object" data-id="voltmeter" data-label="מד מתח"
     data-url="https://he.wikipedia.org/wiki/וולטמטר"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="270.40677" y="790.80865" width="306.75497" height="242.19128" />
  <rect id="hotspot-action-binary-booklet"
     data-hotspot="action" data-action="binary-booklet" data-aria-label="חוברת התרגילים"
     class="hotspot hotspot-action" pointer-events="none"
     style="opacity:0;fill:#ff2fb0" x="845" y="800" width="165" height="125" />

  <script type="text/javascript"><![CDATA[
(function () {
  var PANEL = "panel107_chapter_2_5_workshop";
  function collect() {
    var svg = document.querySelector("svg") || document.documentElement;
    var vb = svg.viewBox && svg.viewBox.baseVal;
    var VW = vb && vb.width ? vb.width : 1448;
    var VH = vb && vb.height ? vb.height : 1086;
    var rootM = null;
    try { rootM = svg.getScreenCTM(); } catch (e) { rootM = null; }
    function box(rect) {
      var bb;
      try { bb = rect.getBBox(); } catch (e) { return null; }
      var m = null;
      try { if (rootM) { m = rootM.inverse().multiply(rect.getScreenCTM()); } } catch (e) { m = null; }
      if (!m) { return { x: bb.x, y: bb.y, w: bb.width, h: bb.height }; }
      function map(x, y) { return { x: m.a * x + m.c * y + m.e, y: m.b * x + m.d * y + m.f }; }
      var p = [map(bb.x, bb.y), map(bb.x + bb.width, bb.y), map(bb.x, bb.y + bb.height), map(bb.x + bb.width, bb.y + bb.height)];
      var xs = [p[0].x, p[1].x, p[2].x, p[3].x], ys = [p[0].y, p[1].y, p[2].y, p[3].y];
      var minX = Math.min.apply(null, xs), maxX = Math.max.apply(null, xs);
      var minY = Math.min.apply(null, ys), maxY = Math.max.apply(null, ys);
      return { x: minX, y: minY, w: maxX - minX, h: maxY - minY };
    }
    function pct(rect) {
      var b = box(rect);
      if (!b || !(b.w > 0) || !(b.h > 0)) return null;
      return { x: b.x / VW * 100, y: b.y / VH * 100, w: b.w / VW * 100, h: b.h / VH * 100 };
    }
    var rects = document.querySelectorAll("[data-hotspot]");
    var out = { panel: PANEL, objects: [], table: null, actions: {} };
    for (var i = 0; i < rects.length; i++) {
      var r = rects[i], kind = r.getAttribute("data-hotspot"), g = pct(r);
      if (!g) continue;
      if (kind === "object") {
        out.objects.push({ id: r.getAttribute("data-id") || r.id || "", label: r.getAttribute("data-label") || "", url: r.getAttribute("data-url") || "", x: g.x, y: g.y, w: g.w, h: g.h });
      } else if (kind === "action") {
        var a = r.getAttribute("data-action");
        if (a) { out.actions[a] = { x: g.x, y: g.y, w: g.w, h: g.h }; }
      }
    }
    return out;
  }
  function send() {
    try {
      if (window.parent && window.parent !== window) {
        window.parent.postMessage({ __warehouseHotspots: true, payload: collect() }, "*");
      }
    } catch (e) {}
  }
  function boot() { send(); setTimeout(send, 120); setTimeout(send, 500); }
  if (document.readyState === "loading") { window.addEventListener("DOMContentLoaded", boot); }
  else { boot(); }
  window.addEventListener("load", send);
})();
]]></script>
</svg>

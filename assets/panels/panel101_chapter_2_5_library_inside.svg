<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg version="1.1" width="1448" height="1086" viewBox="0 0 1448 1086"
   preserveAspectRatio="xMidYMid meet"
   xmlns:xlink="http://www.w3.org/1999/xlink" xmlns="http://www.w3.org/2000/svg">
  <image id="raster-base" x="0" y="0" width="1448" height="1086" preserveAspectRatio="none"
     href="panel101_chapter_2_5_library_inside.jpg" xlink:href="panel101_chapter_2_5_library_inside.jpg" />

  <!-- Editable click-zones. They are invisible (opacity 0) and non-interactive
       here; a script below reads their geometry and posts it to the app, which
       overlays the real clickable areas. Move / resize these in Inkscape. -->
  <rect id="hotspot-object-project-y"
     data-hotspot="object" data-id="project-y" data-label="Project Y"
     data-url="https://en.wikipedia.org/wiki/Project_Y"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="190" y="100" width="175" height="150" />
  <rect id="hotspot-object-physical-review"
     data-hotspot="object" data-id="physical-review" data-label="Physical Review"
     data-url="https://journals.aps.org/pr/issues/64/3-4"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="9" y="836" width="250" height="60" />
  <rect id="hotspot-object-jcp"
     data-hotspot="object" data-id="jcp" data-label="Journal of Chemical Physics"
     data-url="https://pubs.aip.org/aip/jcp/issue/11/1"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="10" y="898" width="262" height="48" />
  <rect id="hotspot-object-rmp"
     data-hotspot="object" data-id="rmp" data-label="Reviews of Modern Physics"
     data-url="https://journals.aps.org/rmp/issues/14/4"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="17" y="948" width="269" height="48" />
  <rect id="hotspot-object-relativity"
     data-hotspot="object" data-id="relativity" data-label="תורת היחסות"
     data-url="https://he.wikipedia.org/wiki/תורת_היחסות"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="1179" y="94" width="42" height="205" />
  <rect id="hotspot-object-quantum"
     data-hotspot="object" data-id="quantum" data-label="מכניקת הקוונטים"
     data-url="https://he.wikipedia.org/wiki/מכניקת_הקוונטים"
     class="hotspot hotspot-object" pointer-events="none"
     style="opacity:0;fill:#22a7ff" x="1305" y="500" width="59" height="290" />
  <rect id="hotspot-action-stone-millis-book"
     data-hotspot="action" data-action="stone-millis-book" data-aria-label="מחברת האריתמטיקה"
     class="hotspot hotspot-action" pointer-events="none"
     style="opacity:0;fill:#ff2fb0" x="980" y="470" width="175" height="340" />

  <script type="text/javascript"><![CDATA[
(function () {
  var PANEL = "panel101_chapter_2_5_library_inside";
  function collect() {
    var svg = document.querySelector("svg") || document.documentElement;
    var vb = svg.viewBox && svg.viewBox.baseVal;
    var VW = vb && vb.width ? vb.width : 1448;
    var VH = vb && vb.height ? vb.height : 1086;
    var rootM = null;
    try { rootM = svg.getScreenCTM(); } catch (e) { rootM = null; }
    function box(rect) {
      var bb;
      try { bb = rect.getBBox(); } catch (e) { return null; }
      var m = null;
      try { if (rootM) { m = rootM.inverse().multiply(rect.getScreenCTM()); } } catch (e) { m = null; }
      if (!m) { return { x: bb.x, y: bb.y, w: bb.width, h: bb.height }; }
      function map(x, y) { return { x: m.a * x + m.c * y + m.e, y: m.b * x + m.d * y + m.f }; }
      var p = [map(bb.x, bb.y), map(bb.x + bb.width, bb.y), map(bb.x, bb.y + bb.height), map(bb.x + bb.width, bb.y + bb.height)];
      var xs = [p[0].x, p[1].x, p[2].x, p[3].x], ys = [p[0].y, p[1].y, p[2].y, p[3].y];
      var minX = Math.min.apply(null, xs), maxX = Math.max.apply(null, xs);
      var minY = Math.min.apply(null, ys), maxY = Math.max.apply(null, ys);
      return { x: minX, y: minY, w: maxX - minX, h: maxY - minY };
    }
    function pct(rect) {
      var b = box(rect);
      if (!b || !(b.w > 0) || !(b.h > 0)) return null;
      return { x: b.x / VW * 100, y: b.y / VH * 100, w: b.w / VW * 100, h: b.h / VH * 100 };
    }
    var rects = document.querySelectorAll("[data-hotspot]");
    var out = { panel: PANEL, objects: [], table: null, actions: {} };
    for (var i = 0; i < rects.length; i++) {
      var r = rects[i], kind = r.getAttribute("data-hotspot"), g = pct(r);
      if (!g) continue;
      if (kind === "object") {
        out.objects.push({ id: r.getAttribute("data-id") || r.id || "", label: r.getAttribute("data-label") || "", url: r.getAttribute("data-url") || "", x: g.x, y: g.y, w: g.w, h: g.h });
      } else if (kind === "action") {
        var a = r.getAttribute("data-action");
        if (a) { out.actions[a] = { x: g.x, y: g.y, w: g.w, h: g.h }; }
      }
    }
    return out;
  }
  function send() {
    try {
      if (window.parent && window.parent !== window) {
        window.parent.postMessage({ __warehouseHotspots: true, payload: collect() }, "*");
      }
    } catch (e) {}
  }
  function boot() { send(); setTimeout(send, 120); setTimeout(send, 500); }
  if (document.readyState === "loading") { window.addEventListener("DOMContentLoaded", boot); }
  else { boot(); }
  window.addEventListener("load", send);
})();
]]></script>
</svg>
